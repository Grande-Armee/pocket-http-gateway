import { createIdApiProperty } from '@shared/docs/properties';

export const UserIdApiProperty = createIdApiProperty({
  description: `User's id.`,
});

export const ResourceIdApiProperty = createIdApiProperty({
  description: `Resource's id.`,
});

export const TagIdApiProperty = createIdApiProperty({
  description: `Tag's id.`,
});

export const UserResourceTagIdApiProperty = createIdApiProperty({
  description: `User resource tag's id.`,
});
